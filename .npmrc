# Supply chain hardening — blocks postinstall scripts.
# When a legitimate package needs to run scripts (e.g. esbuild, sharp, prisma),
# run it explicitly with: npm rebuild <package> --foreground-scripts
# Or for a single install: npm install <pkg> --foreground-scripts
ignore-scripts=true
